name: Act test ci, cd

on:
  pull_request:
    paths:
      - 'act-test/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v5
      with:
        go-version: 1.23
    - run: go test ./...
